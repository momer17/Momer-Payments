spring.application.name=momer-payments-orchestrator

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# JPA configuration
spring.jpa.hibernate.ddl-auto=validate
